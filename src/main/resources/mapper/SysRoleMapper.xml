<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.m7m.omnitranslate.mapper.SysRoleMapper">

    <select id="findByCode" resultType="SysRole">
        select id,role_name,role_code,description,status from sys_role where role_code = #{roleCode}
    </select>

    <select id="findRolesByUserId" resultType="SysRole">
        select r.id,r.role_name,r.role_code,r.description,r.status
            from sys_role r JOIN sys_user_role ur ON r.id = ur.role_id where ur.user_id = #{userId}
    </select>

</mapper>
