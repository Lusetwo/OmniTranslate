<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.m7m.omnitranslate.mapper.SysPermissionMapper">

    <select id="findPermissionsByUserId" resultType="SysPermission">
        select p.id,p.permission_name,p.permission_code from sys_permission p
            JOIN sys_role_permission rp
                ON  p.id = rp.permission_id
            JOIN sys_user_role ur
                ON rp.role_id = ur.role_id
            where ur.user_id = #{userId}
    </select>

</mapper>
